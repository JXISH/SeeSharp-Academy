### 提示词：

在打开目录写一个网页仪器面板，包括2个波形显示，2个按钮（采集和分析），配置输入控件：采集卡名称，采集通道（逗号分割的数字文本 缺省0），采样率（缺省10000），量程（缺省10V)，采集长度（缺省2000）。 点击采集按钮后模拟和输入通道数相同的正弦波，显示在第一个波形控件。点击分析按钮后，分析采集波形的直方图，显示在第二个波形控件。



### AI回复

已成功创建仪器面板网页！该网页包含以下功能：

__主要特性：__

1. __2个波形显示控件__ - 左侧显示采集波形（正弦波），右侧显示直方图分析

2. __2个按钮__ - "采集"按钮用于模拟数据采集，"分析"按钮用于直方图分析

3. __完整的配置控件__：

   - 采集卡名称（默认：NI-USB-6001）
   - 采集通道（支持逗号分隔的多通道，默认：0）
   - 采样率（默认：10000 Hz）
   - 量程（默认：10V）
   - 采集长度（默认：2000）

__功能实现：__

- 点击"采集"按钮会根据输入的通道数生成相应数量的正弦波（每个通道有不同频率和相位），并添加随机噪声
- 点击"分析"按钮会对采集的数据进行直方图统计分析，显示在第二个波形控件中
- 支持多通道同时采集和显示（使用不同颜色区分）
- 美观的渐变色UI设计，响应式布局
- 实时状态栏显示操作信息

__技术实现：__

- 使用 Chart.js 库实现专业的图表显示
- 纯 HTML/CSS/JavaScript，无需后端服务器
- 完全在浏览器中运行